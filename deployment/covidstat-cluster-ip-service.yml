apiVersion: v1
kind: Service
metadata:
  name: covidstat-cluster-ip-service
  labels:
    group: covidstat
spec:
  selector:
    app: covidstat # this tells that the app:covidstat are behind this load-balancer
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080